<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>{{title}} — Just Eat</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, minimal-ui" />
        {{#if cssUrl}}<link rel="stylesheet" href="{{cssUrl}}" />{{/if}}
    </head>

    <body>
